<header id="top-header">
    <span class="siteName">
        <a class="sitetitle" href="/" title="Return to home page">CSE Motors</a>
    </span>
    <div id="tools">
        <% if (locals.loggedin && locals.accountData) { %>
            <a href="/account/" class="welcome-link">
                Welcome <%= locals.accountData.account_firstname %>
            </a>
            <a href="/account/logout">Logout</a>
    <% } %>
    
    <!-- Enlace SOLO cuando el usuario NO está logueado -->
    <% if (!locals.loggedin) { %>
        <a href="/account/login">My Account</a>
    <% } %>
    </div>
</header>